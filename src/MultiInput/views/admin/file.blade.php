<multi-input-file-field
        type="{{ $type }}"
        label="{{ $label }}"
        field="{{ $attribute }}"
        data="{{ $value }}"
        translatable="{{ $translatable }}"
        language="{{ $language }}"
></multi-input-file-field>