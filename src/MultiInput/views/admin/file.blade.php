<multi-input-file-field
        type="{{ $type }}"
        label="{{ $label }}"
        field="{{ $attribute }}"
        data="{{ $value }}"
        translatable="{{ $translatable }}"
        language="{{ $language }}"
        newitemmessage="{{ __('multiinput::admin.file-item-message') }}"
></multi-input-file-field>